on:
  workflow_dispatch:
#   push:
#     branches:
#       - 'uat'
      
name: uat ENV

jobs:
  intergration-test:
    runs-on: ubuntu-latest
    steps:
      - name: Display
        run: |
          bjkhljlkj
    
  release-job:
    runs-on: ubuntu-latest
    needs: intergration-test
    steps:
      - name: Commands
        run: |
            echo "hi"
